[tool.ruff]
src = ["src"]
target-version = "py313"
line-length = 100
lint.select = ["E", "F", "B", "I"]

[tool.ruff.lint.isort]
known-first-party = ["mergemate"]

[tool.ruff.format]
quote-style = "double"
